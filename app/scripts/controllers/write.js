'use strict';

//Write Controller

angular.module('write').controller('WriteCtrl', ['$scope', 'Write', '$timeout', '$window', '$modal',
  function ($scope, Write, $timeout, $window, $modal) {
    
}])

